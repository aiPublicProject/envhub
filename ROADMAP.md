# Roadmap

> 这里记录的是**设计思路与结论**，不是交付承诺，没有时间表。
> 方向可能调整，动手前以最新代码为准。

当前版本（v1.0）只做"加密 .env + 环境注入"。以下两项是讨论定稿但**未实现**的设计，
动手前先读"明确不做"一节的结论，别重新踩坑。

## 1. 统一占位符模型（下一版）

现状：登记时秘密行从 `.env.local` 中删除——文件不完整、必须 gitignore。
目标：秘密行保留，值替换为引用——文件完整且**可提交**。

```
.env.local（可提交 git）              .keyfort（加密）
PORT=3000                             OPENAI_API_KEY=sk-真实值
OPENAI_API_KEY=<keyfort:OPENAI_API_KEY>
```

- 登记动作退化为一个：编辑器选中的行 → 值替换为 `<keyfort:同名>`，行保留
- 运行时唯一管线：解密 → 扫描引用 → 真实值注入环境变量（所有模式共用）
- 依赖 dotenv 家族"**不覆盖已存在环境变量**"的默认行为（python-dotenv / Node
  dotenv / Next.js / Vite / Spring 均如此）：先注入后读文件，占位符永远不被用掉
- `.env.local` 从 gitignore 移除，成为可提交的自文档模板（新卖点：配置可见，值不可见）
- restore 语义 = 把引用换回真实值
- 失败自诊断：忘跑 keyfort 时应用拿到 `<keyfort:` 开头的字符串，一眼可辨病因
- 已知取舍：引用会公开秘密的**名字**（非值），与 sops"键名公开、值加密"哲学一致
- 特殊字符结论：`< > :` 在 dotenv 值 position 安全；刻意避开 `${}` 语法防插值误触；
  遇到挑剔的解析器给值加引号即可（keyfort 解析时本就剥引号）

## 2. 代理模式（二期：进程内无 key）

场景：防 AI / 调试代码**定向提取**运行中的 key（误打印 env 已由注入模型解决）。

设计：本地"注头器"——不实现协议、不解析 body、不理解内容，只做查表+塞头+透传。

```
应用（env 里无 key）──HTTP──> 127.0.0.1 本地代理 ──塞真实认证头──> 真实 API
```

- 路由表存在 `.keyfort` 内，命令配置：`keyfort proxy add / list / remove`
  - 内置预设：openai / anthropic / deepseek（每项：key 名、上游地址、
    注入哪个 `*_BASE_URL`、认证头形态——注意 anthropic 是 `x-api-key` 非 Bearer）
  - 自定义：`keyfort proxy add --key K --upstream URL --env X_BASE_URL --header "X-Api-Key"`
- 注入时按路由表分流：无路由 → 真实值进环境变量（现行为）；有路由 →
  改注入 `*_BASE_URL=http://127.0.0.1:端口/前缀`，真实值只交给代理进程持有
- 流式 / SSE / 工具调用自动支持（字节透传，不理解就无需适配）
- 走代理的 key 在应用 env 里是占位符串——无害，认证头由代理覆盖
- 生命周期：随注入 shell 同生死（进程组绑定），不回锅常驻进程时代
- 覆盖面：HTTP + 头部认证类全部（含全部主流 LLM API），与"实现所有协议"无关

## 明确不做（讨论已定，勿重新发明）

- ❌ **数据库协议代理**：挑战-响应类（MySQL）物理上塞不进凭据；明文握手类（PG）
  要塞就得解析握手 = 实现协议。数据库的正解是**短时凭证**（AWS RDS IAM token
  15 分钟有效等）——未来方向是"运行时刷新短时 token 注入"，不是代理
- ❌ **透明代理 / 自签 CA**（系统级 MITM）：杀毒软件的做法，信任成本不可接受
- ❌ **SDK 包装**（`keyfort(openai)` 式）：逐 SDK 定制违背通用原则，且 key 仍在
  进程内存里，防不住定向提取
- ❌ **集成 LiteLLM 等网关**：重依赖 + 要跟随 provider API 演化维护；
  自写百行注头器更贴合"可审计"品牌
- ❌ **通用多协议 broker**：先例 CyberArk Secretless 已停止活跃维护（归档），
  这条路的维护成本可见一斑，我们不接

## 物理极限（设计时的公理）

进程要出示凭据 ⇒ 凭据必在该进程内存 ⇒ 同进程代码必能读到。
只有"凭据可由第三方代出示"（bearer 头部类）的协议，才能把凭据移出应用进程；
签名 / 握手类协议（AWS SigV4、mTLS、数据库挑战-响应）在此模型下无解，
任何方案都一样——这不是工程问题，是逻辑死结。
